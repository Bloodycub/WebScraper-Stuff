package RandomStuff;

import java.io.File;

import Util.RegionBrain;

public class Timecomp {
	
	 public static void main(String[] args) {
	        // Record start time
	        long startTime = System.currentTimeMillis();

	        test2();

	        // Record end time
	        long endTime = System.currentTimeMillis();

	        // Calculate runtime in milliseconds
	        long runtime = endTime - startTime;
	        System.out.println("Script runtime: " + runtime + " milliseconds");
	    }
	 
	 public void Test1() {
		 
	 }
	 public static void test2() {
		 File TäysOsoiteVerifide = new File("D:\\AAA Scraping\\Scraping fast\\Test\\asd\\a.txt");
		// Check if the Verified directory exists
			File verifiedDir = new File("D:\\AAA Scraping\\Scraping fast\\Test" + " Verified");
			if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}
			if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}if (!TäysOsoiteVerifide.exists()) {
				// Rename the TäysOsoite directory to Verified
				boolean renamed = TäysOsoiteVerifide.renameTo(verifiedDir);
				if (renamed) {
					System.out.println("Folder renamed to Verified successfully.");
				} else {
					System.out.println("Failed to rename the folder to Verified.");
				}
			} else {
				System.out.println("Verified folder already exists.");
			}
	 }
	}

