 package RandomStuff;

public class HowItWorks {

	/* Start Master 
	 * Checks that all regions AND Citys Exicts
	 * Inside Master -> GetKohdenumero()
	 * 		Gets KohdeNumero (Random value started at 1)
	 * 		Return KohdeNumero
	 * Crates Gui in Master
	 * 		Press buttons in Gui
	 * 		OmistusAsunot checked and pressed start
	 * 		if (OmistusAsunot.isSelected()) { ScrapeList.Run(OmistusAsunotKaikki);
	 * GetUrl From Gui to ScrapList
	 * 		GetAllKohdeNumerot  Gets All Kohdenumero That is already readed
	 * 		Creates Workers For Scraping
	 * 		From CrateWorkers-> ScraperWorks(Run)
	 * 		Gets Url And Sends to ListPrint(page);
	 * 			Opens List
	 * 			Forloops ALL array Kohdenumeros
	 * 			Trims till: v42898 Kohdenumero
	 * 				Check If Array Have same KohdeNumero
	 * 				Returns v42898 if Contains
	 * 				else Exporets File with All links
	 *			Done with scraping
	 * Run ScrapList
	 * 		
	 *
	 */
	
	
}
